package net.thunderbird.feature.mail.message.list.ui

/** Selects the visual presentation of the message list while keeping its data and behavior shared. */
enum class MessageListPresentation {
    Default,
    LinusMail,
}
